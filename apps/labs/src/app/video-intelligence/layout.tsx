export const metadata = {
  title: "Video Intelligence",
  icons: { icon: "/video-intelligence.svg" },
};

export default function VideoIntelligenceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
