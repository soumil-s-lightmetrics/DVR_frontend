import DashboardApp from "@/custom-dashboard/ui/DashboardApp";

// Step 1: persona selection is the entry point — there is no landing screen.
export default function PersonaPage() {
  return <DashboardApp screen="persona" />;
}
